{
  "private": true,
  "scripts": {
    "firebase:login": "firebase login",
    "firebase:serve": "firebase emulators:start --only auth,firestore,hosting",
    "firebase:deploy": "firebase deploy"
  },
  "dependencies": {
    "firebase": "^12.11.0",
    "firebase-tools": "^15.13.0"
  }
}
